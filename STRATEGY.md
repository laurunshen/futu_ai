# Futu Paper AI 策略笔记

最后更新：2026-06-11

这个文件只记录交易策略、实验设计和复盘原则。工程进度、部署状态、文件结构和运维命令放在 `PROJECT_CONTEXT.md`。

## 核心判断

本项目的长期目标不是让 AI 直接“保证盈利”，而是建立一个可记录、可复盘、可比较的模拟交易系统：

- 用新闻、行情、持仓和风控生成高质量交易决策。
- 用本地多模拟盘做策略实验和 AB Test。
- 用富途模拟账户做实操验证，但不让多个策略混在同一个富途账户里导致归因混乱。
- 长期积累决策、应用、成交、收益和回撤数据，判断哪些策略真的有正期望。

## 账户分层

### 1. 本地模拟盘

本地模拟盘是主账本和策略实验层。

用途：

- 维护真实持仓成本、现金、仓位和备注。
- 支持多个策略分支，例如手动盘、自动盘、观察盘。
- 记录 AI 决策是否被应用，以及应用后的本地交易流水。
- 做 AB Test 和复盘统计。

当前应用模式：

- `observe`：仅观察，只记录 AI 决策，不改本地持仓。
- `manual`：AI 给建议，用户在 AI 决策详情页手动点击应用。
- `auto`：AI 决策通过风控后，自动应用到本地模拟盘。

### 2. 富途模拟账户

富途模拟账户是实操验证层，不是多策略主账本。

原因：

- 富途 OpenAPI 可以按交易业务账户 `acc_id` 操作，但通常不是让用户自由创建多个策略模拟盘。
- 如果多个本地策略同时同步到同一个富途模拟账户，持仓归因会混乱。
- 因此初期只允许一个本地模拟盘绑定富途模拟账户，作为“富途验证盘”。

建议：

- 本地先跑 Manual vs Auto AB Test。
- 选出稳定策略后，再绑定一个本地盘到富途模拟账户。
- 富途成交结果回写到该本地盘，用于验证真实模拟成交、价格、滑点和执行失败。

## 当前 AB Test 设计

推荐从同一个初始盘克隆出两个分支：

- `l r s - Manual`：AI 只给建议，由用户决定是否应用。
- `l r s - Auto`：AI 满足风控条件后自动应用到本地模拟盘。

两者需要尽量保持：

- 相同初始资金。
- 相同初始持仓。
- 相同行情源。
- 相同新闻源。
- 相同候选池。
- 相同风控参数。

比较指标：

- 总收益率。
- 最大回撤。
- 胜率。
- 盈亏比。
- 持仓天数。
- 交易频率。
- 换手率。
- AI 置信度和实际收益之间的相关性。
- 新闻驱动决策与普通行情驱动决策的表现差异。

## 新闻驱动策略

我们不追求发现量化机构级别的隐藏因子，而是利用及时新闻推送提升决策质量。

新闻需要分层处理：

- 宏观冲击：CPI、FOMC、利率、战争、关税、汇率。
- 行业冲击：AI 算力、半导体出口限制、电商竞争、互联网监管。
- 个股冲击：财报、回购、管理层变化、监管处罚、重大订单。
- 市场情绪：暴跌、放量、异常波动、板块共振。

AI 决策应该区分：

- 新闻事实。
- 基于新闻的推断。
- 当前缺失的数据。
- 可能反转判断的条件。

## 仓位与风控原则

现阶段风控优先级高于收益追求。

原则：

- 现金不足时，宁可 HOLD。
- 高波动宏观环境下，不追高高估值科技股。
- AI 的 `BUY` / `SELL` 必须有明确价格、数量、理由和失效条件。
- 自动应用只允许在本地模拟盘发生，不直接提交富途订单。
- 同一条 AI 决策通过 `decision_id` 防止重复应用。
- 本地模拟盘接近券商购买力逻辑：优先使用交易币种现金，不足时可按汇率从基础币种现金自动换汇扣款。
- 换汇口径必须可复盘：优先使用富途 OpenD FX 快照；如果富途当前不提供可用 FX 行情，则使用本地默认 HKD 汇率表，并把来源写进 AI 决策、模拟盘状态和交易流水。

## 复盘闭环

每条 AI 决策后续都应该逐步追踪：

- 决策时的行情快照。
- 决策时的新闻信号。
- AI 行动：BUY / SELL / HOLD。
- AI 评级：BUY / OVERWEIGHT / HOLD / UNDERWEIGHT / SELL。
- 是否应用：未应用、手动应用、自动应用、应用失败。
- 应用后的 1 天、3 天、7 天表现。
- 是否触发失效条件。
- 最终复盘标签：正确、过早、过晚、误判、风控避免损失。

## 近期策略路线

1. 先把 `l r s` 克隆成 Manual / Auto 两个分支。
2. 给两个分支补齐基础币种现金；买美股时本地账本可按汇率自动折算购买力。
3. 让 AI 周期稳定运行一段时间，只比较本地盘表现。
4. 增加收益曲线和决策后表现追踪。
5. 选一个本地盘作为富途验证盘，再研究富途模拟账户同步。
